import filterStyle from "@/Components/filterStyle";
import React from "react";
import { Text, View } from "react-native";

const filterScreen = () => {
    return (
        <View style={filterStyle.container}>
            <Text>Filter filter filter</Text>
        </View>
    );
};

export default filterScreen;
 